apiVersion: v1
kind: Service

metadata:
  name: railwaygpt-backend
  namespace: railwaygpt

spec:
  selector:
    app: railwaygpt-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: NodePort